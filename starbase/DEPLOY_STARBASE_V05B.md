# Deploy StarBase v5B

1. Upload/replace the repository folder named exactly `starbase`.
2. Do not rename the folder to the ZIP filename and do not add `(1)` to the deployed folder name.
3. Streamlit Main file path remains exactly:

   `starbase/app.py`

4. For certification, leave `Include REVIEW trades` **unchecked**.
5. Open `Josh Fleet Economics + Inventory (v5B)`.
